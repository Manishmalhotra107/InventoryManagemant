<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yashu.arora.inventorymanagement">

    <application
        android:name=".InventoryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventoryManagement">
        <activity
            android:name=".dashboard.UpdateProductActivity"
            android:label="@string/title_activity_update_product"
            android:theme="@style/Theme.InventoryManagement.NoActionBar"></activity>
        <activity
            android:name=".dashboard.AddProduct"
            android:launchMode="singleInstance" />
        <activity
            android:name=".dashboard.DashBoardActivity"
            android:label="@string/dashboard"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login"
            android:launchMode="singleInstance" />
        <activity
            android:name=".SplashActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>